⦁	Configure typescript at system level (optional) 
⦁	Install Nodejs -  https://nodejs.org/en
⦁	Install (Don’t install at global level) 
npm install -g typescript 
npm install -g ts-node 

⦁	Demo on running the typescript file using tsc/node and ts-node 

To work on typescript 
⦁	Install VS Code 
⦁	Create a project and open it in VS Code 
⦁	Set up TypeScript in VS Code 
⦁	Set-ExecutionPolicy RemoteSigned -Scope CurrentUser  (in case of any errors) 
⦁	npm init -y 
⦁	Update package.json 
⦁	"type": "module"
⦁	npm install -D typescript ts-node @types/node 
⦁	npx tsc –init 
⦁	Update tsconfig.json 
